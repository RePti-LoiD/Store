<Page
    x:Class="Store.View.ImagePickPage"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Store.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
        <FlipView x:Name="Gallery" MaxWidth="400" Height="270"/>
        <muxc:PipsPager x:Name="FlipViewPipsPager"
            HorizontalAlignment="Center"
            NumberOfPages="{x:Bind Gallery.Items.Count, Mode=OneWay}"
            SelectedPageIndex="{x:Bind Path=Gallery.SelectedIndex, Mode=TwoWay}" /> 

        <Button Click="PickImageButton_Click" HorizontalAlignment="Center">Pick image</Button>
    </StackPanel>
</Page>
