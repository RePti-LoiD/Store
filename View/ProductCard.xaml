<UserControl
    x:Class="Store.View.ProductCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Store.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Image 
            Width="250" 
            Height="350"
            Stretch="UniformToFill">
            
            <Image.Source>
                <BitmapImage UriSource="{x:Bind Product.Pictures[0]}"/>
            </Image.Source>
        </Image>

        <Grid HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Height="70"
            Grid.Row="1"
            Background="{StaticResource SmokeFillColorDefaultBrush}">

            <TextBlock Text="{x:Bind Product.Name, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
        </Grid>
    </Grid>
</UserControl>
